# MyWebsite

간단한 정적 사이트 초기화.

## 실행

1. 의존성 설치 (옵션 — `live-server` 사용):

```bash
npm install
```

2. 개발 서버 실행:

```bash
npm start
```

또는 그냥 `index.html`을 브라우저로 열어도 됩니다.

## 파일

- `index.html` — 진입점
- `styles.css` — 기본 스타일
- `script.js` — 간단한 인터랙션

